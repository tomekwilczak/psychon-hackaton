import type { MenuEntry } from "../types";

/** Skrzynka pytań od uczestniczek (pakiet H17). */
const entry: MenuEntry = {
  label: "Pytania",
  href: "/prowadzacy/pytania",
  order: 20,
};

export default entry;
