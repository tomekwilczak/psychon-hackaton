import type { MenuEntry } from "../types";

/** Pakiet H20 — dziennik działań. */
const entry: MenuEntry = {
  label: "Dziennik działań",
  href: "/admin/dziennik",
  order: 45,
};

export default entry;
