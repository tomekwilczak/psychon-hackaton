import type { MenuEntry } from "../types";

/** Pakiet H15 — kolejka weryfikacji profili psychologów. */
const entry: MenuEntry = {
  label: "Profile psychologa",
  href: "/admin/profile",
  order: 30,
};

export default entry;
