import type { MenuEntry } from "../types";

/** Pakiet H18 — panel osób i karta osoby. */
const entry: MenuEntry = {
  label: "Uczestniczki",
  href: "/admin/uczestniczki",
  order: 20,
};

export default entry;
