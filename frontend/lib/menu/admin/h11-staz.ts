import type { MenuEntry } from "../types";

/** Pakiet H11 — kolejka akceptacji wpisów stażu. */
const entry: MenuEntry = {
  label: "Akceptacja stażu",
  href: "/admin/staz",
  order: 25,
};

export default entry;
