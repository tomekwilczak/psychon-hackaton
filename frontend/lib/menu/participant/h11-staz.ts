import type { MenuEntry } from "../types";

/** Pakiet H11 — dziennik stażu wolontariusza. */
const entry: MenuEntry = {
  label: "Dziennik stażu",
  href: "/panel/staz",
  order: 30,
};

export default entry;
