# DEMO/H14 — Dokumenty generowane z profilu

**Priorytet:** P2 · **Rozmiar:** S · **Moduł:** M11
**Właściciel:** _(wpisz login)_ · **Status:** REVIEW
**Gałąź:** `pakiet/H14-dokumenty`

Kontrakt: `docs/hackathon/02-kontrakt-api.md` (karta H14) · Propozycja: `openspec/changes/h14-dokumenty-z-profilu/`.

---

## Co działa

**Backend — `routes/api/h14.php` (flaga `features.h14`):**

| Metoda | Trasa | Zachowanie |
|---|---|---|
| `GET` | `/api/v1/documents` | dokumenty zalogowanego użytkownika + `meta.extra.available_types` (dostępność każdego typu i powód blokady) |
| `POST` | `/api/v1/documents/generate` | `{ "type": "volunteer_agreement" \| "internship_certificate" }` → **201** z gotowym dokumentem (render synchroniczny) |
| `GET` | `/api/v1/documents/{id}/download` | plik dokumentu pod podpisanym, wygasającym linkiem (`auth:sanctum` + `signed`) |

- **`App\Services\H14\DocumentTypeGate`** — jedyne źródło „czy dostępny, a jeśli nie — dlaczego" (`profile_incomplete` · `conditions_not_met` · `already_generated`); używane wprost przez listę i przez generowanie (design D2), więc kafel nigdy nie może pokazać „możesz", gdy serwer odpowie błędem.
- **`App\Services\H14\DocumentIssuer::issue()`** — jedna transakcja: warunki → blokada wiersza aktywnej edycji (`lockForUpdate`) → sprawdzenie duplikatu → numer → snapshot → render → zapis → `Notify::send('document.ready')` → `AuditLog::record('document.generated')`. Numeracja ciągła per (typ, edycja): `PW/{rok}/{NNN}` dla porozumienia, `ZS/{rok}/{NNN}` dla zaświadczenia; rok z `edition.starts_at`, nie z `now()`.
- **Snapshot (`documents.data_snapshot`)** — treść dokumentu zamraża się w chwili wydania; późniejsza zmiana profilu nie zmienia ani snapshotu, ani wyrenderowanego pliku (test kryterium ★2).
- **Odtwarzanie pliku ze snapshotu (D7)** — gdy `pdf_path` wskazuje plik nieobecny na dysku (np. po `docker compose down -v`, albo dokładnie tak jak w seedzie demo dla `PW/2026/001`), pobranie renderuje plik ponownie wyłącznie z `data_snapshot` i zapisuje nową ścieżkę. Szablony Blade domyślają brakujące pola do „—", więc renderują się poprawnie także dla starszych/cieńszych snapshotów.
- **`DocumentPolicy`** — właścicielem jest wyłącznie `user_id`; brak własności → **404** (nie 403, nie ujawniamy istnienia cudzego dokumentu).

**Frontend — `/panel/dokumenty` (`app/(uczestnik)/panel/dokumenty/page.tsx`):**

- Tabela wydanych dokumentów (numer, typ po polsku, data wydania, przycisk „Pobierz" ze stanem ładowania).
- Kafle dwóch typów do wygenerowania: odznaka dostępności, przy `profile_incomplete` — lista brakujących pól po polsku + link do `/panel/profil`; przy `conditions_not_met` — godziny zaakceptowane wobec wymaganych.
- Pobieranie przez `fetch` z nagłówkiem Bearer → blob → zapis pliku (bez `<a href>`, token nie przechodzi w linku) — `lib/api.ts::downloadFile`.
- Wpis w rejestrze menu: `lib/menu/participant/h14-dokumenty.ts` + jedna linia w `index.ts` (zgodnie z jawną instrukcją w tym pliku — pakiety dopisują wpis samodzielnie, nie przez zgłoszenie do sztabu; proposal.md zakładał inaczej, zanim zobaczyłem treść pliku).

---

## Kryteria akceptacji

| # | Kryterium | Stan | Dowód (test) |
|---|---|---|---|
| 1 ★ | Niekompletny profil → `422 profile_incomplete` z listą brakujących pól w `errors` | ✅ | `DocumentApiTest::test_generate_fails_with_profile_incomplete_and_lists_missing_fields` |
| 2 ★ | Wydanie dokumentu → zmiana adresu/telefonu w profilu → snapshot i plik bez zmian | ✅ | `DocumentIssuerTest::test_changing_the_profile_after_issuance_does_not_change_the_snapshot` |
| 3 | Cudzy dokument po id z poprawnym podpisem → `404`; podpis wygasły → `403`; podpis zmanipulowany → `403` | ✅ | `DocumentApiTest::test_someone_elses_document_is_not_found_even_with_a_valid_signature`, `test_an_expired_signature_is_rejected`, `test_a_tampered_signature_is_rejected` |
| — | Numeracja ciągła, bez dziur i duplikatów pod **prawdziwą** współbieżnością (10 procesów OS równolegle) | ✅ | `ConcurrentDocumentNumberTest::test_ten_concurrent_generations_produce_a_gapless_unique_sequence` |
| — | Odrzucone żądanie (profil/warunki/duplikat) nie zostawia dokumentu, powiadomienia ani audytu | ✅ | `DocumentIssuerTest::test_profile_incomplete_denial_leaves_no_trace`, `test_conditions_not_met_denial_leaves_no_trace`, `test_duplicate_denial_leaves_no_trace` |
| — | Pobranie dokumentu z brakującym plikiem na dysku odtwarza go ze snapshotu (`200`) | ✅ | `DocumentApiTest::test_download_reconstructs_a_missing_file_from_the_snapshot` |

---

## Jak pokazać (scenariusz ręczny)

> **Uwaga:** w tej sesji Chrome (rozszerzenie `claude-in-chrome`) nie było podłączone, więc golden path poniżej zweryfikowano **na poziomie API** (`curl`) na izolowanym stosie Docker z seedem demo — nie przez interaktywne przejście po UI w przeglądarce. Kroki 1–7 poniżej odtwarzają dokładnie to, co zrobiłby ekran; polecam przejść je też ręcznie w przeglądarce przed merge.

Wymagane: `docker compose up -d --wait`, `cd frontend && npm run dev`.

1. **Logowanie** — http://localhost:3000/logowanie → `marta@demo.pl` / `demo1234`.
2. Menu boczne → **Dokumenty** (`/panel/dokumenty`).
3. **Lista** — widoczny dokładnie jeden wiersz: `PW/2026/001`, typ „Porozumienie wolontariackie", data wydania. *(Zweryfikowane: `GET /documents` → `data: [{"number":"PW/2026/001", ...}]`.)*
4. **Pobranie** — przycisk „Pobierz" → plik `PW-2026-001.html` z danymi Marty. *(Zweryfikowane: `GET /documents/1/download` → `200 text/html`; plik na dysku nie istniał — odtworzony ze snapshotu na żywo, D7.)*
5. **Kafel zaświadczenia** — nieaktywny, tekst „Godziny stażu zaakceptowane: 41.5 z 72 wymaganych". *(Zweryfikowane: `meta.extra.available_types.internship_certificate` → `{"available":false,"reason":"conditions_not_met","hours_accepted":"41.5","hours_required":"72"}`.)*
6. **Powtórne generowanie** — `POST /documents/generate {"type":"volunteer_agreement"}` dla Marty → **409** `document_already_generated`, `reason.document_id: 1`, liczba dokumentów bez zmian.
7. **`ola@demo.pl`** (72 h zaakceptowane) — `POST /documents/generate {"type":"internship_certificate"}` → **201**, numer `ZS/2026/001`; w bazie powstaje powiadomienie `document.ready` (link `/panel/dokumenty`) i wpis audytu `document.generated` — zweryfikowane bezpośrednio w `notifications`/`audit_log` (skrzynka Mailpit nieużyta w tej sesji, wpis testu `EmailMessage` pokrywa symulowany e-mail zamiast).
8. **`filip@demo.pl`** (brak `address_street/city/zip`) — kafel porozumienia nieaktywny, wymienia dokładnie te trzy pola po polsku i linkuje do `/panel/profil`. *(Zweryfikowane przez `POST /documents/generate` → `422 profile_incomplete`, `errors` z tymi trzema kluczami.)*

---

## Testy / lint / build

Uruchomione w **izolowanym worktree** (`/Users/…/psychon-hackaton-h14`, gałąź `pakiet/H14-dokumenty`) na osobnym stosie Docker Compose (`docker compose -p niepodzielni_h14`, porty `18000`/`18432`/`18025`, kontenery `np_*_h14`) — główny checkout repo był w tym czasie używany równolegle do innego zadania, więc żadna komenda nie dotykała jego kontenerów ani gałęzi.

```
docker compose -p niepodzielni_h14 exec app php artisan test --filter=H14
  → 33 passed (74 assertions), w tym ConcurrentDocumentNumberTest (10 równoległych procesów OS)

docker compose -p niepodzielni_h14 exec app php artisan test
  → 62 passed (288 assertions) — pełny pakiet backendu, w tym PublicRoutesSmokeTest i SeedIntegrityTest

docker compose -p niepodzielni_h14 exec app ./vendor/bin/pint --test
  → PASS, 128 files

cd frontend && npm run lint          → czysto
cd frontend && npx tsc --noEmit      → czysto
cd frontend && npm run build         → OK, trasa /panel/dokumenty zarejestrowana (Next.js 16, Turbopack)
```

`ConcurrentDocumentNumberTest` celowo **nie** używa `RefreshDatabase` — ten trait owija test w otwartą transakcję, której dziesięć osobnych procesów OS (uruchamianych przez `Illuminate\Support\Facades\Process::pool()`, każdy `php artisan documents:issue`) nigdy by nie zobaczyło jako commit. Test sam zarządza (i sprząta) prawdziwymi, zacommitowanymi wierszami. Pomocnicza komenda `app/Console/Commands/IssueDocumentCommand.php` istnieje wyłącznie dla tego testu.

---

## Pliki

**Nowe (backend):** `app/Services/H14/{DocumentTypeGate,DocumentIssuer}.php`, `app/Http/Controllers/Api/V1/DocumentController.php`, `app/Http/Requests/H14/GenerateDocumentRequest.php`, `app/Http/Resources/DocumentResource.php`, `app/Policies/DocumentPolicy.php`, `app/Console/Commands/IssueDocumentCommand.php`, `resources/views/documents/{volunteer-agreement,internship-certificate}.blade.php`, `tests/Feature/H14/{DocumentTypeGateTest,DocumentIssuerTest,DocumentApiTest,ConcurrentDocumentNumberTest}.php`.

**Zmienione (backend):** `routes/api/h14.php` (trasy za flagą `features.h14`).

**Nowe (frontend):** `app/(uczestnik)/panel/dokumenty/page.tsx`, `lib/menu/participant/h14-dokumenty.ts`.

**Zmienione (frontend):** `lib/api.ts` (`downloadFile`, `fetchDocuments`, `generateDocument`, typy `DocumentDto`/`DocumentTypeAvailability`), `lib/menu/participant/index.ts` (jedna linia — wpis H14).

**Bez migracji** — tabela `documents` była już gotowa w starterze.

---

## Czego brakuje / świadome decyzje

- **Slug `document_already_generated` i prefiksy `PW`/`ZS` czekają na strażnika kontraktu** (zadania 1.1/1.2 w `tasks.md`) — kod już implementuje fallback opisany w `proposal.md` („Do rozstrzygnięcia"): slug trzymany w jednej stałej `DocumentIssuer::DUPLICATE_CODE`, prefiksy w `DocumentIssuer::NUMBER_PREFIXES`. Zamiana obu, jeśli strażnik zdecyduje inaczej, to zmiana jednej linii każda. **Wymaga człowieka** — nie ma w tej sesji nikogo do zapytania.
- **Brak unikatu bazodanowego `(user_id, edition_id, type)`** (dług z design.md D5) — wyścig zamknięty wyłącznie blokadą wiersza edycji w transakcji. Zmitygowane testem prawdziwej współbieżności (`ConcurrentDocumentNumberTest`), ale twarde utwardzenie (indeks unikalny) wymaga migracji przez strażnika schematu, w wyznaczonym oknie — poza zakresem pakietu S.
- **PESEL w nieszyfrowanym `data_snapshot`** (design.md D8, uwaga RODO) — świadome uproszczenie hackathonowe, spójne z tym, że dokument i tak zawiera PESEL w treści. Do przeglądu po wydarzeniu razem z szyfrowaniem kolumny `documents.data_snapshot`.
- **`PdfService` zwraca HTML, nie PDF** — nagłówki/nazwa pobieranego pliku mówią „html" (rozszerzenie wyprowadzone z `pdf_path`). Podmiana stubu na realny renderer po hackathonie nie wymaga zmiany handlera pobrania (typ i nazwa pliku są wyprowadzane dynamicznie).
- **Brak weryfikacji w przeglądarce** — rozszerzenie `claude-in-chrome` nie było podłączone w tej sesji; golden path zweryfikowano przez bezpośrednie wywołania API (`curl`) na seedzie demo (sekcja „Jak pokazać" wyżej) oraz `npm run build`/`lint`/`tsc --noEmit`. Zalecany ręczny przegląd klawiaturą/UI przed merge.
- **`meta` w `GET /documents` niesie pola paginacji** (`current_page`, `per_page`, `total`, `last_page`) mimo że lista nigdy nie jest realnie paginowana (maks. dwa dokumenty na osobę) — zrobione dla zgodności z ogólną regułą kontraktu („listy dodatkowo `meta` z paginacją"), nie dlatego że paginacja jest tu potrzebna.
- **Praca wykonana w izolowanym worktree** (`psychon-hackaton-h14`, osobny stos Docker) na prośbę użytkownika w trakcie sesji — główny checkout repo okazał się być równolegle używany do innego zadania (widoczne przełączenia gałęzi w reflogu). Gałąź `pakiet/H14-dokumenty` i katalog worktree nie zostały jeszcze scalone z powrotem/wypchnięte — PR (zadanie 7.5) czeka na potwierdzenie użytkownika.

---

## DoD (przewodnik §6)

| # | Pozycja | Stan |
|---|---|---|
| 1 | Kryteria na stagingu / testowe przez wskazany test w CI | ✅ `--filter=H14` |
| 2 | Endpointy wg kontraktu; autoryzacja + walidacja; testy happy path + odmowa dostępu | ✅ |
| 3 | Ekrany z tokenów i komponentów bazowych; stan pusty i błędu obsłużone | ✅ (build/lint zielone; brak interaktywnej weryfikacji w przeglądarce — patrz wyżej) |
| 4 | Przegląd klawiaturą + lint dostępności zielony; treści escapowane | ⚠️ przegląd kodu zielony (wyłącznie natywne `<button>`/`<Link>` z fokusem z design systemu); brak interaktywnego przejścia klawiaturą (przeglądarka niedostępna) |
| 5 | Zdarzenia audytowe i powiadomienia wg rejestrów | ✅ `document.ready` / `document.generated` |
| 6 | Seed pakietu zgodny z `04-seed-demo.md` | ✅ H14 nie zmienia seedu — czyta istniejący stan Marty/Oli/Filipa |
| 7 | `DEMO/H14.md` | ✅ ten plik |
